# Daisy 1.0.7.32 — Home layout fix

## Fixed

- The Home screen no longer collapses into a single stacked column when no calendar is connected. The two-column layout is now constant — without a calendar the day card simply shows less inside it, staying in its place on the right.
