# Daisy 1.0.7.48 — clearer model costs

## Fixed

- The per-model cost column now uses one shared decimal precision. Small model costs no longer round to $0.00 while the monthly total looks like one model’s amount; the visible rows now add up to the total.
