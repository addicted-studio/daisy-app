// Placeholder. Switched to shields.io static badge in Hero +
// Download instead of a runtime GitHub API fetch — keeps Hero
// as a synchronous server component, no auth headers, no
// rate-limit risk. If we ever want exact star counts with our
// own typography (rather than shields.io's), reintroduce a
// build-time fetch here and pass via prop.

export {};
